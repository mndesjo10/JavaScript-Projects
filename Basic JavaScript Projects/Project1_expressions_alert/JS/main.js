// 1. Declare two number variables
var Sent1 = "This is the begining of the string";
var Sent2 = " and this is the end of the string.";

// 2. Concatenate the two strings together
var fullSentence = Sent1 + Sent2;

// 3. Use window.alert() to display the concatenated string
window.alert("Alert: " + fullSentence);

// 4. Use document.write() to display the sentece on the page
document.write("<h2>concatenated String:</h2>");
document.write(fullSentence);

// 5. Write an expression (mathematical or logical)
var number1 = 10;
var number2 = 5;
var sum = number1 + number2; // Expression: addition
document.write("<p><br>Epression Result: " + number1 + " + " + number2 + " = " + sum + "</p>");

